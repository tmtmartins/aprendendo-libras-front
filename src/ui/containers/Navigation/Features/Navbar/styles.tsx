import styled from "styled-components";
import { Nav, NavDropdown } from "react-bootstrap";

export const StyledNavbar = styled(Nav)`
  border-radius: 400px;
`;

export const StyledNavbarDropdown = styled(NavDropdown)`
  border-radius: 3px;
`;
